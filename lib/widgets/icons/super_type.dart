import 'package:flutter/material.dart';

class SuperTypeIcon extends StatelessWidget {
  late double height;
  SuperTypeIcon({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
